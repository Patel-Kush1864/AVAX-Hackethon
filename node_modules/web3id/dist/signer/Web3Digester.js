"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Web3Digester = void 0;
const ethers_1 = require("ethers");
const Web3Encoder_1 = require("./Web3Encoder");
const EtherWallet_1 = require("./EtherWallet");
const lodash_1 = __importDefault(require("lodash"));
/**
 * 	@class Web3Digester
 */
class Web3Digester {
    /**
     *	@param obj		{ any }
     *	@param exceptedKeys	{ Array<string> }
     *	@returns {Promise<string>}
     */
    static hashObject(obj, exceptedKeys) {
        return new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
            try {
                if (!obj) {
                    return reject(`invalid obj`);
                }
                if (!EtherWallet_1.EtherWallet.isValidAddress(obj.wallet)) {
                    return reject(`invalid obj.wallet`);
                }
                if (!lodash_1.default.isNumber(obj.timestamp) || obj.timestamp <= 0) {
                    return reject(`invalid obj.timestamp`);
                }
                const message = yield Web3Encoder_1.Web3Encoder.encode(obj, exceptedKeys);
                const arrayBuffer = new TextEncoder().encode(message);
                const keccakHash = (0, ethers_1.keccak256)(arrayBuffer);
                //	will return a string starting with 0x and having a length of 66
                resolve(keccakHash.trim().toLowerCase());
            }
            catch (err) {
                reject(err);
            }
        }));
    }
    /**
     *	@param hash	{any}
     *	@returns {boolean}
     */
    static isValidHash(hash) {
        return lodash_1.default.isString(hash) && !lodash_1.default.isEmpty(hash) && (0, ethers_1.isHexString)(hash, 32);
    }
}
exports.Web3Digester = Web3Digester;
//# sourceMappingURL=Web3Digester.js.map